# {{ ansible_managed }}

[Unit]
Description=Docker for run {{ SERVICE }}
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker start -a {{ SERVICE }}
ExecStop=/usr/bin/docker stop -t 20 {{ SERVICE }}

[Install]
WantedBy=multi-user.target
